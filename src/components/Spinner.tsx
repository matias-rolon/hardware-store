import './styles/spinner.css'; // Asegúrate de tener un archivo CSS para el estilo del spinner

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
